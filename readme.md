# para teste digite python app.py no terminal, o login é admin e senha 123
# Caso não rode, rode o seguinte comando
python -m venv venv
./venv/Scripts/Activate.ps1
pip install PySimpleGUI
pip install supabase
python app.py